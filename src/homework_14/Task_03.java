/*package homework_14;

public class Task_03 {
    public static void main(String[] args) {

        System.out.println(substring((str, 2, 5 ));
    }

    public static String  substring(String[] strings, int startIdx, int endIdx) {
        String result = "";
               if(strings == null || startIdx < 0|| startIdx >= string.length || endIdx >= strings.length || endIdx <= startIdx) return result;
             char[]chars = new char[endIdx - startIdx];


             for (int i = 0; i < chars.length; i++) {
                 chars[i] = string.charAt(i + startIdx);
             }
        for (int i = startIdx; i < endIdx; i++) {
            result += "" + string.charAt(i);

            return String.valueOf(chars);
    }
}


 */